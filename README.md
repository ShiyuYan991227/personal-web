# Shiyu Yan Personal Website

这是严诗钰个人网页的 GitHub Pages 发布包。

## 文件结构

- `index.html`：网页入口文件，GitHub Pages 会自动读取它。
- `assets/`：网页图片与 PDF 文章附件。

## 更新方式

如果之后要更新网页内容，只需要替换 `index.html` 和 `assets/` 中对应文件，然后重新上传到 GitHub 仓库。
